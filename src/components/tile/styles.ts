import { StyleSheet } from "react-native";

export const TileStyles = StyleSheet.create({
  container: {
    borderWidth: 5,
    width: 100,
    height: 100,
    justifyContent: "center",
    alignItems: "center",
  },
  text: {
    fontSize: 80,
    fontWeight: "bold",
  },
});
